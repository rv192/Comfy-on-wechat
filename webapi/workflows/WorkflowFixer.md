因为需要替换工作流模板中的参数，为了避免动态替代的不稳定性，需要提前对工作流模板的节点编号做处理，固化下来。以下是提示词，在Gemini-2.0-flash-exp下测试通过。
需要人工确定原工作流模板中的节点ID，替换下面提示词的中{}，除了{save_imageID}是必填外，其他3个在不同工作流中可能不存在。

工作提示词：
把以下工作流模板中的提示词节点{promptID}替换成1，{latentID}替换成2，{seedID}替换成3，{save_imageID}替换成99
替换过程中注意除了直接节点外，还需要替换前后关联节点

```
{原工作流模板}
```